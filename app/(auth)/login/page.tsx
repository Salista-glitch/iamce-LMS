import { LoginForm } from '@/components/forms/login-form';

export const metadata = {
  title: 'Sign In - Learnthru',
  description: 'Sign in to your Learnthru account',
};

export default function LoginPage() {
  return <LoginForm />;
}
